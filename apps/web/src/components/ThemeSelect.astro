---
// No-op override of Starlight's ThemeSelect.
//
// BulletinMail ships a single light theme — the broadsheet paper palette —
// and intentionally does not offer a switcher. The audience is small-org
// admins who prefer stability over toggles. Rationale: /DESIGN.md §4.
//
// Registered via astro.config.mjs → starlight({ components: { ThemeSelect } }).
---
